#include <iostream>
#include <cstdlib>
#include <cassert>

// Custom structure for holding computation dimention
struct ComputeDimentions {
    size_t matrix_rows;
    size_t matrix_cols;
    size_t vector_size;
}